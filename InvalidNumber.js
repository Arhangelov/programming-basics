function invalidNumber (input){
    let n = Number(input.shift());

    if (n === 0){

    }
    else if ((n < 100)||(n > 200) ){
        console.log ("invalid");
    }
    else if ((n >= 100) || (n <= 200)){
        
    }
}

invalidNumber ([
    0
])